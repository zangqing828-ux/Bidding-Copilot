const makeEntry = (status, owner, workPackage, source = 'webBridge') => ({
  status,
  owner,
  workPackage,
  source,
});

module.exports = {
  version: 'wp-a-contract-manifest-v1',
  methods: {
    app: {
      getVersion: makeEntry('pending', 'web-shell', 'WP-A'),
      getGpuHardwareAccelerationStatus: makeEntry('pending', 'web-shell', 'WP-A'),
      saveGpuHardwareAccelerationPreference: makeEntry('pending', 'web-shell', 'WP-A'),
      startGpuHardwareAccelerationTrial: makeEntry('pending', 'web-shell', 'WP-A'),
      relaunchWithGpuHardwareAccelerationDisabled: makeEntry('pending', 'web-shell', 'WP-A'),
      getLatestVersion: makeEntry('pending', 'web-shell', 'WP-A'),
      getUpdateDownloadUrl: makeEntry('pending', 'web-shell', 'WP-A'),
      checkUpdate: makeEntry('pending', 'web-shell', 'WP-A'),
      startUpdate: makeEntry('pending', 'web-shell', 'WP-A'),
      quitAndInstall: makeEntry('pending', 'web-shell', 'WP-A'),
    },
    requiredOnlineServices: {
      getStatus: makeEntry('pending', 'runtime', 'WP-A'),
    },
    config: {
      load: makeEntry('implemented', 'settings', 'WP-A'),
      save: makeEntry('implemented', 'settings', 'WP-A'),
      listModels: makeEntry('pending', 'settings', 'WP-A'),
      openConfigFolder: makeEntry('pending', 'settings', 'WP-A'),
    },
    license: {
      getStatus: makeEntry('pending', 'settings', 'WP-A'),
      refresh: makeEntry('pending', 'settings', 'WP-A'),
      importOfflineFile: makeEntry('pending', 'settings', 'WP-A'),
      activateOfflineCode: makeEntry('pending', 'settings', 'WP-A'),
    },
    ai: {
      chat: makeEntry('pending', 'runtime', 'WP-A'),
      requestJson: makeEntry('pending', 'runtime', 'WP-A'),
      testImageModel: makeEntry('pending', 'runtime', 'WP-A'),
    },
    agent: {
      listRuntimes: makeEntry('pending', 'runtime', 'WP-A'),
      run: makeEntry('pending', 'runtime', 'WP-A'),
      selfCheck: makeEntry('pending', 'runtime', 'WP-A'),
      exportSelfCheckReport: makeEntry('pending', 'runtime', 'WP-A'),
      getStatus: makeEntry('pending', 'runtime', 'WP-A'),
      restart: makeEntry('pending', 'runtime', 'WP-A'),
    },
    developerTokenStats: {
      openWindow: makeEntry('pending', 'developer', 'WP-A'),
      get: makeEntry('pending', 'developer', 'WP-A'),
      reset: makeEntry('pending', 'developer', 'WP-A'),
    },
    developerExpansionReplaceTest: {
      run: makeEntry('pending', 'developer', 'WP-A'),
    },
    file: {
      selectDuplicateCheckFiles: makeEntry('pending', 'workflow', 'WP-A'),
    },
    knowledgeBase: {
      getMigrationStatus: makeEntry('implemented', 'knowledge', 'WP-A'),
      migrateLegacy: makeEntry('implemented', 'knowledge', 'WP-A'),
      list: makeEntry('pending', 'knowledge', 'WP-A'),
      createFolder: makeEntry('pending', 'knowledge', 'WP-A'),
      renameFolder: makeEntry('implemented', 'knowledge', 'WP-A'),
      reorderFolder: makeEntry('implemented', 'knowledge', 'WP-A'),
      deleteFolder: makeEntry('implemented', 'knowledge', 'WP-A'),
      deleteDocument: makeEntry('implemented', 'knowledge', 'WP-A'),
      moveDocument: makeEntry('implemented', 'knowledge', 'WP-A'),
      uploadDocuments: makeEntry('pending', 'knowledge', 'WP-A'),
      retryDocument: makeEntry('implemented', 'knowledge', 'WP-A'),
      startMatching: makeEntry('pending', 'knowledge', 'WP-A'),
      readMarkdown: makeEntry('implemented', 'knowledge', 'WP-A'),
      readItems: makeEntry('implemented', 'knowledge', 'WP-A'),
      readAnalysis: makeEntry('implemented', 'knowledge', 'WP-A'),
    },
    technicalPlan: {
      loadState: makeEntry('implemented', 'technical-plan', 'WP-A'),
      importTenderDocument: makeEntry('pending', 'technical-plan', 'WP-A'),
      importOriginalPlanDocument: makeEntry('pending', 'technical-plan', 'WP-A'),
      checkBidSections: makeEntry('implemented', 'technical-plan', 'WP-A'),
      selectBidSection: makeEntry('implemented', 'technical-plan', 'WP-A'),
      readTenderMarkdown: makeEntry('implemented', 'technical-plan', 'WP-A'),
      readTenderSourceMarkdown: makeEntry('implemented', 'technical-plan', 'WP-A'),
      readOriginalPlanMarkdown: makeEntry('implemented', 'technical-plan', 'WP-A'),
      updateStep: makeEntry('implemented', 'technical-plan', 'WP-A'),
      setWorkflowKind: makeEntry('implemented', 'technical-plan', 'WP-A'),
      switchWorkflowKind: makeEntry('implemented', 'technical-plan', 'WP-A'),
      saveBidAnalysisConfig: makeEntry('implemented', 'technical-plan', 'WP-A'),
      saveOutlineConfig: makeEntry('implemented', 'technical-plan', 'WP-A'),
      saveOutline: makeEntry('implemented', 'technical-plan', 'WP-A'),
      saveGlobalFacts: makeEntry('implemented', 'technical-plan', 'WP-A'),
      saveContentGenerationOptions: makeEntry('implemented', 'technical-plan', 'WP-A'),
      saveChapterContent: makeEntry('implemented', 'technical-plan', 'WP-A'),
      clear: makeEntry('implemented', 'technical-plan', 'WP-A'),
    },
    duplicateCheck: {
      loadState: makeEntry('implemented', 'workflow', 'WP-A'),
      saveFiles: makeEntry('implemented', 'workflow', 'WP-A'),
      saveUiState: makeEntry('implemented', 'workflow', 'WP-A'),
      updateState: makeEntry('implemented', 'workflow', 'WP-A'),
      clear: makeEntry('implemented', 'workflow', 'WP-A'),
    },
    rejectionCheck: {
      loadState: makeEntry('implemented', 'workflow', 'WP-A'),
      importDocument: makeEntry('pending', 'workflow', 'WP-A'),
      importTenderFromTechnicalPlan: makeEntry('implemented', 'workflow', 'WP-A'),
      removeDocument: makeEntry('implemented', 'workflow', 'WP-A'),
      saveUiState: makeEntry('implemented', 'workflow', 'WP-A'),
      updateState: makeEntry('implemented', 'workflow', 'WP-A'),
      clear: makeEntry('implemented', 'workflow', 'WP-A'),
    },
    templates: {
      list: makeEntry('implemented', 'workflow', 'WP-A'),
      get: makeEntry('implemented', 'workflow', 'WP-A'),
      create: makeEntry('implemented', 'workflow', 'WP-A'),
      update: makeEntry('implemented', 'workflow', 'WP-A'),
      delete: makeEntry('implemented', 'workflow', 'WP-A'),
    },
    tasks: {
      startBidSectionExtraction: makeEntry('pending', 'workflow', 'WP-A'),
      startBidAnalysis: makeEntry('pending', 'workflow', 'WP-A'),
      startOutlineGeneration: makeEntry('pending', 'workflow', 'WP-A'),
      startGlobalFactsGeneration: makeEntry('pending', 'workflow', 'WP-A'),
      startContentGeneration: makeEntry('pending', 'workflow', 'WP-A'),
      pauseContentGeneration: makeEntry('pending', 'workflow', 'WP-A'),
      startRejectionItemsExtraction: makeEntry('pending', 'workflow', 'WP-A'),
      startRejectionCheck: makeEntry('pending', 'workflow', 'WP-A'),
      startDuplicateAnalysis: makeEntry('pending', 'workflow', 'WP-A'),
      getActiveTasks: makeEntry('implemented', 'workflow', 'WP-A'),
    },
    export: {
      exportWord: makeEntry('pending', 'export', 'WP-A'),
      openFile: makeEntry('pending', 'export', 'WP-A'),
    },
    systemFonts: {
      list: makeEntry('pending', 'web-shell', 'WP-A'),
    },
    resources: {
      list: makeEntry('removed', 'legacy', 'WP-A', 'deleted-product'),
    },
    tenderOpportunities: {
      list: makeEntry('removed', 'legacy', 'WP-A', 'deleted-product'),
    },
    plugins: {
      list: makeEntry('removed', 'legacy', 'WP-A', 'deleted-product'),
    },
  },
};
